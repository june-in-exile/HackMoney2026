{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../sdk && npm install && npm run build && cd ../frontend && npm install && npm run build",
  "installCommand": "npm install",
  "rootDirectory": "frontend"
}
