{
  "installCommand": "npm install && cd sdk && npm install && cd ../frontend && npm install",
  "buildCommand": "cd sdk && npm run build && cd ../frontend && npm run build",
  "devCommand": "cd frontend && npm run dev",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs"
}
