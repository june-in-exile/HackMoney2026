# Dependencies
node_modules/

# Build artifacts (large files)
build/*.ptau
build/*.zkey
build/*.wasm
build/*.wtns
build/*.r1cs
build/*.sym
build/*_js/
build/*.log

# Keep hex outputs and JSON for reference
!build/*.hex
!build/*.json